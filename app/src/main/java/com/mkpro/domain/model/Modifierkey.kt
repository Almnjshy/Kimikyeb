package com.mkpro.domain.model

/**
* Enum class representing modifier keys on a keyboard
*/
enum class ModifierKey {
   SHIFT,
   CTRL,
   ALT,
   META,  // Windows key / Command key
   FN     // Function key
}